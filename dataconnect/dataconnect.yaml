specVersion: "v1"
serviceId: "notesapp"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "notesapp-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
