import React from "react";
import NavBar from "../navbar/NavBar";

const Notes: React.FC = () => {
    return (
        <>
            <NavBar />
            <main>Notes Page</main>
        </>
    );
};

export default Notes;
